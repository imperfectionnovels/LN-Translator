/* reader-quality.js — in-app quality cockpit (per-chapter badge +
 * learn-from-edits panel). Populated in Phase 2/3 of the cockpit work.
 * Loaded after the other reader-*.js modules so it can call into them. */
